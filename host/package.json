{
  "name": "host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run build:api && pnpm run build:plugins && tsc -p tsconfig.json",
    "build:api": "cargo run --manifest-path ../plugins/crates/patchouli-plugin-api/Cargo.toml --features ts-export --bin export_ts",
    "build:plugins": "cargo build --manifest-path ../plugins/builtin/status/Cargo.toml --release --target wasm32-unknown-unknown && cargo build --manifest-path ../plugins/extra/joke/Cargo.toml --release --target wasm32-unknown-unknown",
    "start": "node --env-file=.env dist/main.js",
    "dev": "tsc -p tsconfig.json --watch",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "~6.0.2",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "packageManager": "pnpm@11.1.3",
  "dependencies": {
    "discord.js": "^14.26.4"
  }
}
