[package]
name = "modulebot-extra-joke-interceptor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
modulebot-plugin-api = { path = "../../crates/modulebot-plugin-api" }

[profile.release]
opt-level = "s"
strip = true
lto = true
